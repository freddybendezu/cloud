apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: azure-vote-front1
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: azure-vote-front1
    spec:
      containers:
      - name: azure-vote-front1
        image: freddybendezu/demo01:ver1
        ports:
        - containerPort: 8761
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m
        env:
        - name: REDIS1
          value: "azure-vote-back1"
---
apiVersion: v1
kind: Service
metadata:
  name: azure-vote-front1
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: azure-vote-front1